
import { Period } from "../items/Period";
import { headerFactory } from "./Header";

export const PeriodHeader = null // this has no edit...

export function PeriodHeaderControl({ id }: Model & React.Attributes) {
  return (
   /* <Header {...{ id, title: 'periods', after: <Period {...{ id }} /> }}  />*/<></>
  )
}
